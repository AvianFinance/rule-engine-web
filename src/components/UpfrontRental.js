import React from 'react';

const UpfrontRental = () => {
  

//   const handleClick = (page) => {
//     console.log('clicked');
//     history.push(`/${page}`)
//   }

  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', height: '100vh' }}>
      <h1 style={{ textAlign: 'center', marginBottom: '50px' }}>Upfront Renatals Smart Contract</h1>
     
    </div>
  );
};

export default UpfrontRental;