import React from 'react';

const InstallmentRental = () => {
  

//   const handleClick = (page) => {
//     console.log('clicked');
//     history.push(`/${page}`)
//   }

  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', height: '100vh' }}>
      <h1 style={{ textAlign: 'center', marginBottom: '50px' }}>Installment Rental Smart Contract</h1>
    </div>
  );
};

export default InstallmentRental;